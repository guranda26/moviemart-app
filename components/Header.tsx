import React from "react";

const Header = () => {
  return (
    <section>
      <header
        style={{
          backgroundColor: "lightblue",
          padding: "1rem",
        }}
      >
        <p>Header</p>
      </header>
    </section>
  );
};

export default Header;
